# Integrations

ModerateAI supports three integration surfaces:

- Website Widget + Lead Capture
- Telegram
- Discord

Each workspace can use all three platform types, with destination counts enforced by plan entitlements.
